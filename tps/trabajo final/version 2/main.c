#include "filtro.h"
#include "imagen.h"
#include "pixel.h"
#include "validacion.h"


//input output filter crop horizontal vertical

int main(int argc, char* argv[])
{




    /* imagen_t * entrada;
    extension_t tipo_archivo_entrada, tipo_archivo_salida;

    FILE* fi = validar_input(argc, argv, &tipo_archivo_entrada);
    if(fi==NULL)
    {
        fprintf(stderr, "error en le archivo definido como input\n");
            return EXIT_FAILURE;
    }
    
    FILE* fo = validar_output(argc, argv, &tipo_archivo_salida);
    if(fo==NULL)
    {
        fprintf(stderr, "error en le archivo definido como output\n");
            return EXIT_FAILURE;
    }

    if(tipo_archivo_entrada == PPM)
    {
        entrada = imagen_leer_ppm(fi);
    }else{
        entrada = imagen_leer_BMP(fi);
    }

    entrada = validar_recorte(argc, argv, f);
        if(entrada==NULL)
        {
            free(entrada);
            
        }
*/
    return 0;
}
